{
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /rag-and-embeddings/",
    "export": "slidev export --format pptx --output ../rag-and-embeddings.pptx --with-clicks=false"
  },
  "dependencies": {
    "@slidev/cli": "^51.0.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.49.1"
  }
}
